// Campus Cartel tasks. The screen itself is shared — ambassadors reach the same
// one from (ambassador)/tasks, and the audience filtering is driven by role.
export { default } from '../../src/screens/shared/TasksScreen';
