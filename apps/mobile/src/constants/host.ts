/** Organisation sectors offered during host sign-up and on the host profile. */
export const SECTORS = [
  'Technology', 'Education', 'Finance', 'Healthcare', 'Media', 'Entertainment',
  'Retail', 'Manufacturing', 'Consulting', 'Non-Profit', 'Government', 'Other',
] as const;

/** Event categories a host can tag a proposal with. */
export const EVENT_CATEGORIES = ['Social', 'BFSI', 'Corporate', 'Other'] as const;
